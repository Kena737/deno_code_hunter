import Blog from '../../models/Blog';
import connectDb from '../../middleware/mongoose';

const handler = async (req, res) => {
    // update the blog with example id

}

export default connectDb(handler);
    